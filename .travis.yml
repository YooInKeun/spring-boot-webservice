# .travis.yml
language: java
jdk:
  - openjdk8

branches:
  only:
    - main

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build" # master 브랜치에 푸시 되었을 때 수행되는 명령어

# CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      - keun0390@gmail.com
  #slack: miridih:63vC2SIecsMGvyJAjijHRhU2